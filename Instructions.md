# Do these before you run the script

- First run :
  `pip install -r requirements.txt`

- Then craete a folder to store your data like this:
  ![alt text](image.png)

- Run the script using:
  `python record.py`

- Then you select that particular folder from this browse menu:
  ![alt text](image-1.png)

- A raw folder will be created inside the folder of your choosing.
